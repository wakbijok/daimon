[package]
name = "daimon-app"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "daimon web application — Leptos full-stack"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
leptos = { version = "0.8" }
leptos_router = { version = "0.8" }
leptos_meta = { version = "0.8" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
web-sys = { version = "0.3", features = ["Window", "Location", "Document", "HtmlDocument", "HtmlElement", "WebSocket", "MessageEvent", "CloseEvent", "ErrorEvent", "BinaryType", "Storage", "MediaQueryList", "Navigator", "Clipboard", "KeyboardEvent", "HtmlInputElement", "File", "FileList", "FileReader", "Blob", "ProgressEvent", "EventTarget"], optional = true }
console_error_panic_hook = { version = "0.1", optional = true }
wasm-bindgen = { version = "0.2", optional = true }
wasm-bindgen-futures = { version = "0.4", optional = true }
gloo-timers = { version = "0.3", features = ["futures"], optional = true }
js-sys = { version = "0.3", optional = true }

# Server-only dependencies
axum = { version = "0.8", features = ["ws"], optional = true }
tokio = { version = "1", features = ["rt-multi-thread"], optional = true }
leptos_axum = { version = "0.8", optional = true }
# Phase 2c D3b: rusqlite removed, daimon-db wraps tokio-postgres + deadpool.
daimon-db = { path = "../daimon-db", optional = true }
tokio-postgres = { version = "0.7", features = ["with-uuid-1", "with-chrono-0_4", "with-serde_json-1"], optional = true }
jsonwebtoken = { version = "9", optional = true }
bcrypt = { version = "0.17", optional = true }
rand = { version = "0.9", optional = true }
daimon-pve = { path = "../daimon-pve", optional = true }
futures = { version = "0.3", optional = true }
tracing = { version = "0.1", optional = true }

# Phase 2b #19: in-process broker stack. daimon-app is a permitted wrapper
# per `deny.toml` (same rationale as daimon-cli). `for-broker` opens the
# internal API surface on the gated three crates.
daimon-broker = { path = "../daimon-broker", optional = true }
anyhow = { version = "1", optional = true }
chrono = { version = "0.4", optional = true }

# Phase 3 #9: memory + RAG for /admin/memory admin UI.
daimon-memory = { path = "../daimon-memory", optional = true }
daimon-rag = { path = "../daimon-rag", optional = true }

[features]
hydrate = [
    "leptos/hydrate",
    "dep:console_error_panic_hook",
    "dep:wasm-bindgen",
    "dep:wasm-bindgen-futures",
    "dep:web-sys",
    "dep:gloo-timers",
    "dep:js-sys",
]
ssr = [
    "dep:axum",
    "dep:tokio",
    "dep:leptos_axum",
    "leptos/ssr",
    "leptos_router/ssr",
    "dep:daimon-db",
    "dep:tokio-postgres",
    "dep:jsonwebtoken",
    "dep:bcrypt",
    "dep:rand",
    "dep:daimon-pve",
    "dep:futures",
    "dep:tracing",
    "dep:daimon-broker",
    "dep:anyhow",
    "dep:chrono",
    "dep:daimon-memory",
    "dep:daimon-rag",
]
